#局部变量
  1.函数内部的变量名如果第一次出现，且出现在=前面，即被视为定义一个局部变量，不管全局域中有没有用到该变量名，函数中使用的将是局部变量
  2.函数的参数也会被定义为局部变量
#全局变量
  1.全局变量可以在函数中被访问，但是如果被赋值的话，就会满足上面第一条，函数内部会重新定义一个同名的局部变量，因此该赋值并不会影响全局变量的值
  2.如果要在函数内部更改全部变量的值，需要在变量前面加上global关键字，标记此变量是全局变量
